enum Days {
  MONDAY = 'Monday',
  TUESDAY = 'Tuesday',
  WEDNESDAY = 'Wednesday',
  THURSDAY = 'Thursday',
  FRIDAY = 'Friday',
  SATURDAY = 'Saturday',
  SUNDAY = 'Sunday',
}

enum TimeType {
  START = 'start',
  END = 'end'
}

enum PromiseType {
  FULFILLED = 'fulfilled',
  REJECTED = 'rejected',
}

enum RateTypeValue {
  HOURLY = 'hourly',
  DAILY = 'daily'
}

export { Days, TimeType, PromiseType, RateTypeValue }