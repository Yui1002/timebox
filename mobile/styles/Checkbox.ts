import {StyleSheet} from 'react-native';
import {COLORS} from './theme';

export default class CheckboxStyle {
  static createBasicCheckboxStyle() {
    return StyleSheet.create({
      checkbox: {
        width: 20,
        height: 20,
        marginHorizontal: 8
      },
    }).checkbox;
  }
}
