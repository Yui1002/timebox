import React, {useState} from 'react';
import {Text, View, SafeAreaView, TextInput, Button} from 'react-native';
import {styles} from '../styles/starterStyles.js';

const Starter = ({navigation}: any) => {


  return (
    <SafeAreaView>
        <View>
            
        </View>
    </SafeAreaView>
  );
};

export default Starter;
